name: GraceCompanion
version: 0.1.0-SNAPSHOT
main: dev.grace.companion.GraceCompanionPlugin
api-version: 1.19
description: Grace anticheat companion plugin

config:
  bridge:
    host: 127.0.0.1 # -> if running proxy on sep machine, use proxy ip
    port: 27016 # make sure this port is open / not blocked by firewall